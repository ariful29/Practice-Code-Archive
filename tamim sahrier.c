#include<stdio.h>

int main()
{

    /*
     *Print double value
       double d;
       scanf("%lf", &d);
       printf("%.2lf", d);

      *multiplication of 2 double value
      double a,b,product;
      printf("Enter two numbers to multiplication: ");
      scanf("lf %lf", &a, &b);

      product = a * b;

      printf("Product = %.2lf", product);

      *print ascii value from input char value
      char c;
      printf("Enter a character: ");
      scanf("%c", &c);
      printf("ASCII value of %c = %d", c,c);

       *Program to Compute Quotient and Remainder
       int dividend, divisor, quotient, remainder;
       printf("Enter dividend: ");
       scanf("%d", &dividend);
       printf("Enter divisor:");
       scanf("%d", &divisor);

       quotient = dividend / divisor;

       remainder = dividend % divisor;

       printf("Quotient = %d\n", quotient);
       printf("Remainder = %d", remainder);

    *Program to Find the Size of Variables

    int intType;
    float floatType;
    double doubleType;
    char charType;

    printf("Size of int: %zu bytes\n", sizeof(intType));
    printf("Size of float: %zu bytes\n", sizeof(floatType));
    printf("Size of double: %zu bytes\n", sizeof(doubleType));
    printf("Size of char: %zu bytes\n", sizeof(charType));


   double first, second, temp;
   printf("Enter first number: ");
   scanf("%lf", &first);
   printf("Enter second number: ");
   scanf("lf", &second);

   temp = first;
   first = second;
   second = temp;

   printf("\nafter swap first number = %.2lf\n", first);
   printf("after swap second number = %.2lf", second);
 */

    return 0;

}
